package userDAO;

public class TeacherDTO extends UserDTO {

    public TeacherDTO() {}

    public TeacherDTO(String userId, String userName, String password) {
        super(userId, userName, password);
    }
}
